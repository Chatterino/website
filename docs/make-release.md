# Checklist for bumping a release

- [ ] Update the `current_version` value in `links.json`
- [ ] Add the previous release version to the `versions` list in `links.json` (this list feeds the "Old Builds" page and excludes the current version)
- [ ] Add a filtered list of changelog entries to `pages/changelog.md.j2`
